{
  "name": "svg-favicon-generator",
  "version": "1.0.0",
  "description": "React + Vite SVG favicon generator for emoji, unicode symbol, and text-based icons.",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview",
    "test:sanitize": "node scripts/test-sanitize.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^8.0.3"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "preact": "^10.14.0"
  }
}
